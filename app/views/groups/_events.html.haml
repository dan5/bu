%table
  - @events.each do |event|
    %tr
      %td .
      - t = event.started_at
      %td== #{t.month}/#{t.day}
      %td= link_to event.group.name, event.group
      %td= link_to event.title, event
      %td 未入力
      %td
        = link_to '出席'
        = link_to '欠席'
        = link_to '微妙'
