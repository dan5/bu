%h1 My page

%h2 活動スケジュール
- @events = @user.groups.map(&:events).flatten.each
= render '/groups/events'

%h2 参加している部活
%ul
  - @user.groups.each do |group|
    %li
      = link_to group.name, group
      == ...
      = group.description
